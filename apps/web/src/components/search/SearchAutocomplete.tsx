// Re-exports SearchAutocomplete — implementation is in SearchAutocompleteImpl.tsx
// The original file path is preserved for backwards compatibility with all existing imports.
export { default } from './SearchAutocompleteImpl';
